package pathing.pathing;

public class jackob {
}



/*public static class Paths {

  public PathChain Path1;
  public PathChain Path2;
  public PathChain Path3;
  public PathChain Path4;
  public PathChain Path5;

  public Paths(Follower follower) {
    Path1 = follower
      .pathBuilder()
      .addPath(
        new BezierLine(new Pose(43.000, 8.000), new Pose(63.000, 80.000))
      )
      .setLinearHeadingInterpolation(Math.toRadians(90), Math.toRadians(137))
      .build();

    Path2 = follower
      .pathBuilder()
      .addPath(
        new BezierLine(new Pose(63.000, 80.000), new Pose(42.000, 84.000))
      )
      .setTangentHeadingInterpolation()
      .build();

    Path3 = follower
      .pathBuilder()
      .addPath(
        new BezierLine(new Pose(42.000, 84.000), new Pose(25.000, 84.000))
      )
      .setTangentHeadingInterpolation()
      .build();

    Path4 = follower
      .pathBuilder()
      .addPath(
        new BezierLine(new Pose(25.000, 84.000), new Pose(57.000, 87.000))
      )
      .setTangentHeadingInterpolation()
      .setReversed(true)
      .build();

    Path5 = follower
      .pathBuilder()
      .addPath(
        new BezierLine(new Pose(57.000, 87.000), new Pose(54.000, 90.000))
      )
      .setTangentHeadingInterpolation()
      .build();
  }
}

 */